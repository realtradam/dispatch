node_modules/
dist/
build/
*.tsbuildinfo

# Local test credentials — never commit.
.env

# Agent prompts (orchestrator→subagent) and work reports — local only.
prompts/
reports/

# OS / editor noise
.DS_Store
*.log
